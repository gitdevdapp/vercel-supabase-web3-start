# Supabase Configuration (CANONICAL MJR PROJECT)
# Get these values from: https://supabase.com/dashboard/project/[REDACTED-PROJECT-ID]/settings/api
NEXT_PUBLIC_SUPABASE_URL=https://[REDACTED-PROJECT-ID].supabase.co
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_OR_ANON_KEY=your-anon-key-here

# Optional: Service Role Key (for admin operations - keep secret!)
# SUPABASE_SERVICE_ROLE_KEY=your-service-role-key-here

# CDP (Coinbase Developer Platform) Configuration
# Get these from your Coinbase Developer Platform dashboard
# Get credentials from: https://portal.cdp.coinbase.com/
CDP_API_KEY_ID=your-cdp-api-key-id
CDP_API_KEY_SECRET=your-cdp-api-key-secret
NEXT_PUBLIC_WALLET_NETWORK=base-sepolia

# ERC721 Deployment Configuration
# Private key of a funded account on Base Sepolia (with at least 0.01 ETH)
# Used to deploy real ERC721 contracts to Base Sepolia testnet
# FORMAT: 0x followed by 64 hex characters (no leading 0x in some formats)
CDP_DEPLOYER_PRIVATE_KEY=0xyour-funded-deployer-private-key-here

# AI Service Configuration
OPENAI_API_KEY=your-openai-key

# Feature Flags (disabled by default for safety)
NEXT_PUBLIC_ENABLE_CDP_WALLETS=false
NEXT_PUBLIC_ENABLE_AI_CHAT=false
NEXT_PUBLIC_ENABLE_WEB3_AUTH=false

# Web3 Authentication Configuration
WEB3_AUTH_SECRET=your-signing-secret-here
WALLET_CONNECT_PROJECT_ID=your-wallet-connect-project-id-optional
